name: factory-config-builder
channels:
  - conda-forge
dependencies:
  - python=3.10
  - streamlit=1.24.0
  - pip
  - openjdk=17
  - git   # optional, handy to have
  - pip:
      - toml  # optional helper libs if you extend the app
